# projentiq
ProjentIQ - AI document extraction &amp; knowledge base platform

## Landing page

Nuxt 4 / SSG (`nuxt generate`). Spec: [`.claude/projentiq_landing_SPEC.md`](.claude/projentiq_landing_SPEC.md).

```bash
npm install
npm run dev        # http://localhost:3000
npm run generate   # statický build do .output/public
```
